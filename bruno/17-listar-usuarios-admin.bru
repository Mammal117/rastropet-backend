meta {
  name: 17 - Listar usuarios (admin)
  type: http
  seq: 17
}

get {
  url: {{base_url}}/users
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
}