meta {
  name: 02 - Me (ruta protegida)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/auth/me
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
}